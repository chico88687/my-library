import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.my.library',
  appName: 'my-library-app',
  webDir: 'dist/my-library-app/browser',
};

export default config;
